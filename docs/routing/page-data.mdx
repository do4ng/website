`$page` variable provides data of page.

Example:

```ts
export const $page = {};
```

---

## `$page.before/after`

A function that is run before/after processing request.

```ts
export const $page = {
  before(ctx) {
  }
  after(ctx) {
  }
}
```
