[Source](https://github.com/zely-js/zely/blob/zely3/packages/zely-js-cli/commands/start.ts), `zely start -h`

---

`start` is a command that runs the app in production mode.

```bash
zely-cli start
```

<Box type="warning">
  There is currently a bug where the `--serpack` flag does not work.
</Box>

## Arguments

| **option** | **description** | **default** |
| ---------- | --------------- | ----------- |
