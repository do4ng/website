Main module for zely-js

[source](https://github.com/zely-js/zely/tree/zely3/packages/zely-js), [npm](https://www.npmjs.com/package/@zely-js/zely)

## Usage

```ts
import { zely } from '@zely-js/zely';

zely({
  /* options */
}).then((app) => {
  app.listen(8080);
});
```
